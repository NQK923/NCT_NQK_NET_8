export interface ModelBanner {
  id_Banner?: number;         // Giữ nguyên tên theo API
  url_manga: string;        // Giữ nguyên tên theo API
  image_banner: string;     // Giữ nguyên tên theo API
  datePosted: Date;         // Có thể giữ nguyên hoặc chuyển đổi về Date
}
